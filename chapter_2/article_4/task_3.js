const birthday = '18.04.1982';
// можно преобразовать в BIRTHDAY, т. к. значние предопределено, а не вычисляется

const age = someCode(birthday);
// нельзя преобразовать в AGE, т. к. является вычисляемой в процессе выполнения (зависит от birthday)